# Invalid test configuration for main_test.go
# Missing required nbuserver section - triggers validation error

server:
  host: "localhost"
  port: "8080"
  uri: "/metrics"
  logName: "/dev/null"
  scrapingInterval: "5m"

# Missing nbuserver section entirely - required for valid config
