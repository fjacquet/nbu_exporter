name: CI
on:
  push: { branches: [main] }
  pull_request:
permissions:
  contents: read
jobs:
  ci:
    uses: fjacquet/ci/.github/workflows/go-ci.yml@v1
  security:
    uses: fjacquet/ci/.github/workflows/go-security.yml@v1
